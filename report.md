# 편향 보정과 기지국 재선택을 활용한 RTT 실내 위치 추정 알고리즘

## 1. 모티베이션 & 인트로

본 프로젝트는 18개 기지국이 측정한 RTT 거리값과 기지국 좌표를 이용하여 사용자의 2차원 위치를 추정하는 문제를 다룬다. 입력 데이터는 각 사용자에 대해 18개의 거리 측정값을 제공하며, 목표는 실제 사용자 위치에 가까운 추정 좌표를 계산하는 것이다.

단순한 다변측량에서는 모든 기지국의 측정값을 한 번에 사용하여 위치를 계산한다. 그러나 실제 실내 환경에서는 반사, 비가시선 환경, 신호 지연, 측정 잡음 등으로 인해 일부 기지국의 RTT 측정값이 크게 왜곡될 수 있다. 따라서 모든 기지국을 동일하게 신뢰하는 방식은 특정 이상 측정값에 의해 최종 위치가 크게 흔들릴 수 있다는 한계가 있다.

중간발표까지의 실험에서는 WiFi 및 UWB 기반 실내 위치 추정 과정에서 AP별 편향 보정, 가중치 기반 삼변측량, 이상치 완화 방식 등을 검토하였다. 이 과정에서 측정거리에는 AP 또는 기지국마다 일정하게 치우치는 편향이 존재할 수 있고, 일부 측정값은 실제거리와 크게 어긋날 수 있음을 확인하였다. 특히 평균 기반 보정은 큰 이상치에 민감하므로, 중앙값을 이용한 편향 보정이 더 안정적이라고 판단하였다.

Final Project 데이터에서도 유사한 문제가 관찰되었다. 제공된 학습 데이터에서 실제 사용자 위치와 기지국 좌표를 이용해 실제 기하학적 거리를 계산하고, 이를 RTT 측정값과 비교한 결과, RTT 측정값은 실제 거리보다 전반적으로 크게 나타나는 양의 편향을 보였다. 전체 거리 측정값의 오차가 작지 않았기 때문에, RTT 값을 그대로 다변측량에 사용하는 것은 적절하지 않다고 판단하였다.

본 보고서에서는 이러한 문제를 해결하기 위해 **편향 보정과 기지국 재선택을 활용한 RTT 실내 위치 추정 알고리즘**을 제안한다. 이하에서는 이를 **제안 알고리즘**이라고 부른다. 제안 알고리즘의 핵심 아이디어는 세 가지이다. 첫째, 기지국별 median 편향의 절반만 제거하여 RTT 측정값의 양의 편향을 완화한다. 둘째, 가까운 기지국 후보군에서 기지국을 하나씩 제외하며 여러 위치 후보를 생성하고, 이 후보들의 중앙값을 초기 위치로 사용한다. 셋째, 초기 위치와 잘 맞는 기지국을 잔차 기준으로 다시 선택한 뒤, 강건 가중치를 적용하여 최종 위치를 보정한다.

제안 알고리즘은 학습 기반 모델처럼 정답 좌표를 직접 예측하는 방식이 아니다. 학습 데이터의 정답 좌표는 기지국별 편향 상수를 계산하는 데만 사용하고, 실제 위치 추정 단계에서는 RTT 측정값과 기지국 좌표만을 사용한다. 따라서 Random Forest와 같은 지도학습 모델에 비해 train 데이터에 강하게 맞춰지는 위험을 줄이고, hidden test set에서도 동일한 결정론적 절차로 동작하도록 설계하였다.

## 2. 알고리즘 설명

사용자 위치를 $p=(x,y)$, 기지국 $i$의 좌표를 $p_{bs,i}$, 기지국 $i$가 측정한 RTT 거리값을 $\hat d_i$라고 하자. 이상적인 경우에는 다음 관계가 성립한다.

$$
\hat d_i \approx \|p-p_{bs,i}\|
$$

그러나 실제 측정값에는 편향과 이상치가 포함될 수 있으므로, 제안 알고리즘은 먼저 기지국별 편향을 보정한 뒤, 가까운 기지국 후보군을 구성하고, 잔차 기준으로 기지국을 다시 선택하여 강건한 위치 추정을 수행한다.

먼저 제공된 학습 데이터에서 각 기지국별 median 편향을 계산한다. 사용자 $u$의 실제 위치를 $p_u$라고 하면, 기지국 $i$와 사용자 $u$ 사이의 실제 기하학적 거리는 다음과 같다.

$$
d_{true,i,u}=\|p_u-p_{bs,i}\|
$$

기지국 $i$의 측정 오차는 다음과 같이 정의한다.

$$
e_{i,u}=\hat d_{i,u}-d_{true,i,u}
$$

각 기지국의 편향은 평균이 아니라 중앙값으로 계산한다.

$$
b_i = \mathrm{median}_{u} \left( \hat d_{i,u} - d_{true,i,u} \right)
$$

중앙값을 사용하는 이유는 일부 RTT 측정값이 크게 튀는 경우에도 편향 추정값이 과도하게 흔들리지 않도록 하기 위함이다. 다만 $b_i$를 전부 제거하면 모든 위치에서 동일한 편향이 발생한다고 가정하는 것이므로, hidden test에서 과보정이 발생할 수 있다. 따라서 제안 알고리즘에서는 편향의 절반만 제거하는 보수적 보정을 적용하였다.

$$
\tilde d_i=\hat d_i-0.5b_i
$$

여기서 $\tilde d_i$는 편향 보정 후의 거리값이다. 이 보정은 RTT 측정값의 양의 편향을 줄이되, 전체 편향을 강제로 제거하지 않도록 설계한 절충 방식이다.

편향 보정 후에는 $\tilde d_i$가 작은 순서대로 가까운 기지국 5개를 초기 후보군으로 선택한다. 이 후보군을 $S_0$라고 하면, $S_0$는 다음과 같이 정의된다.

$$
S_0=\{\tilde d_i \text{가 작은 상위 5개 기지국의 index}\}
$$

가까운 기지국을 우선 선택한 이유는 사용자와 가까운 기지국이 위치 추정에 더 직접적인 정보를 줄 가능성이 높기 때문이다. 그러나 가까운 기지국 중 하나가 큰 오차를 포함할 수도 있으므로, 선택된 기지국 전체를 그대로 한 번만 사용하는 대신 기지국 제외 일관성 검사를 수행한다.

먼저 $S_0$ 전체를 사용하여 하나의 위치 후보를 계산한다.

$$
\hat{p}_{S_{0}}
=
\arg\min_{p}
\sum_{i \in S_{0}}
\left(
\|p-p_{bs,i}\|-\tilde{d}_{i}
\right)^2
$$

그 다음 $S_{0}$에 포함된 기지국을 하나씩 제외하며 추가 위치 후보를 만든다. 기지국 $k$를 제외한 후보는 다음과 같이 계산된다.

$$
\hat{p}_{-k}
=
\arg\min_{p}
\sum_{i \in S_{0},\, i \neq k}
\left(
\|p-p_{bs,i}\|-\tilde{d}_{i}
\right)^2
$$

초기 후보군의 크기가 5개일 때, 전체 후보 1개와 leave-one 후보 5개가 생성된다. 이 후보들의 집합을 $\mathcal{P}$라고 하면 다음과 같다.

$$
\mathcal{P}
=
\left\{
\hat{p}_{S_{0}},
\hat{p}_{-1},
\hat{p}_{-2},
\hat{p}_{-3},
\hat{p}_{-4},
\hat{p}_{-5}
\right\}
$$

각 후보 위치는 선형 다변측량으로 초기해를 구한 뒤, 거리 잔차를 줄이는 비선형 보정을 통해 계산한다. 이 부분은 최종 위치를 직접 결정하는 단계라기보다, 여러 개의 위치 가설을 생성하는 단계이다. 특정 기지국 하나가 위치 추정을 크게 왜곡하는 경우, 그 기지국이 제외된 후보는 다른 후보들과 다른 경향을 보일 수 있다. 따라서 여러 후보 위치를 비교하면 특정 기지국 하나에 과도하게 의존하지 않는 초기 위치를 만들 수 있다.

생성된 후보 위치들은 평균이 아니라 좌표별 중앙값으로 결합한다. 후보 위치 $\hat{p}_{k}$를 $(\hat{x}_{k},\hat{y}_{k})$라고 하면 초기 위치 $p_{init}$는 다음과 같다.

$$
p_{init}
=
\left(
\mathrm{median}(\hat{x}_{k}),
\mathrm{median}(\hat{y}_{k})
\right)
$$

평균 대신 중앙값을 사용한 이유는 일부 후보 위치가 크게 벗어나더라도 초기 위치가 함께 끌려가지 않도록 하기 위함이다. 즉, 초기 위치는 여러 후보가 반복적으로 가리키는 중심 위치로 해석할 수 있다.

초기 위치가 계산되면, 전체 18개 기지국에 대해 잔차를 다시 계산한다.

$$
r_{i}
=
\left|
\|p_{init}-p_{bs,i}\|-\tilde{d}_{i}
\right|
$$

이 잔차가 작다는 것은 해당 기지국의 보정 거리값이 현재 초기 위치와 잘 맞는다는 의미이다. 반대로 잔차가 큰 기지국은 현재 위치와 양립하기 어려운 측정값을 가진 것으로 볼 수 있다. 따라서 제안 알고리즘은 잔차가 작은 기지국 8개를 선택하여 최종 위치 보정에 사용한다.

$$
S_{r}
=
\{r_{i} \text{가 작은 상위 8개 기지국의 index}\}
$$

마지막으로 선택된 기지국 집합 $S_{r}$에 대해 강건 보정을 수행한다. 최종 위치는 다음 목적함수를 줄이는 방향으로 반복 갱신된다.

$$
\hat{p}
=
\arg\min_{p}
\sum_{i \in S_{r}}
w_{i}
\left(
\|p-p_{bs,i}\|-\tilde{d}_{i}
\right)^2
$$

여기서 $w_{i}$는 잔차가 큰 측정값의 영향을 낮추기 위한 코시형 강건 가중치이다.

$$
w_{i}
=
\frac{1}{1+\left(\frac{r_{i}}{2s}\right)^2}
$$

scale $s$는 잔차의 MAD를 이용해 계산한다.

$$
s=1.4826\cdot MAD(r)+0.5
$$

MAD를 사용한 이유는 표준편차보다 이상치에 덜 민감하기 때문이다. 최종적으로 제안 알고리즘은 편향 보정, 기지국 제외 일관성, 잔차 기반 기지국 재선택, 강건 가중치 보정을 순차적으로 결합한다. 이 점에서 모든 기지국을 한 번에 사용하는 단순 최소제곱법이나, 기지국별 가중치만 조정하는 일반적인 가중 최소제곱법과 구분된다.

제안 알고리즘은 기존 robust localization 연구와 유사하게 잔차가 큰 측정값의 영향을 줄인다는 문제의식을 공유한다. 그러나 기존 LTS 계열처럼 처음부터 잔차가 작은 기지국 subset만 선택하는 것이 아니라, 먼저 가까운 기지국 후보군에서 leave-one 위치 후보들을 만들고, 그 후보들의 중앙값으로 초기 위치를 구성한 뒤 잔차 기반 재선택을 수행한다는 점에서 차이가 있다. 또한 robust regression 또는 least median 방식과 달리, 제안 알고리즘은 측정값 자체의 median이 아니라 위치 후보들의 좌표별 median을 초기 위치로 사용한다.

### 기존 연구와의 차이

제안 알고리즘은 아래 참고 연구들과 문제의식은 공유하지만, 해당 방법을 그대로 구현한 것은 아니다. 참고 논문들은 NLOS 오차 완화, 잔차 기반 측정값 선별, median 기반 강건 추정의 근거로 사용하였다. 제안 알고리즘은 이 아이디어들을 Final Project 데이터 구조에 맞게 결합하여, 기지국별 절반 편향 보정, 기지국 제외 위치 후보 생성, 후보 위치 중앙값, 잔차 기반 기지국 재선택으로 구성하였다.

| 참고 연구 | 해당 연구의 핵심 내용 | 제안 알고리즘과의 차이 |
|---|---|---|
| Wang et al. | UWB/RTT 기반 실내 위치 추정에서 NLOS 환경이 거리 측정값에 양의 오차를 만들 수 있고, 이를 완화하기 위해 잔차 분석, 가중치 조정, 오차 보정 등이 사용될 수 있음을 정리하였다. | 제안 알고리즘은 별도의 NLOS 식별 모델을 학습하지 않는다. 대신 학습 데이터에서 기지국별 median 편향을 계산하고, 그 절반만 제거하는 보수적 편향 보정을 적용한다. 즉, survey에서 정리한 여러 방법 중 오차 보정과 잔차 분석의 문제의식을 단일 snapshot RTT 데이터에 맞게 단순화하였다. |
| Khodjaev et al. | LTS 기반으로 잔차가 작은 측정값 subset을 선택하여 NLOS 오차의 영향을 줄이는 방법을 제안하였다. | 제안 알고리즘도 잔차가 작은 기지국을 최종 보정에 사용한다는 점은 유사하다. 그러나 처음부터 잔차 subset만 선택하지 않고, 먼저 가까운 기지국 후보군에서 기지국을 하나씩 제외하며 여러 위치 후보를 만든다. 이후 후보 위치들의 중앙값으로 초기 위치를 구성하고, 그 초기 위치를 기준으로 잔차가 작은 기지국을 다시 선택한다. |
| Casas et al. | NLOS 측정값이 포함된 실내 위치 추정에서 robust regression 및 least median 계열 방법으로 이상 측정값의 영향을 줄이는 방법을 제안하였다. | 제안 알고리즘도 중앙값이 이상치에 강하다는 관점을 활용한다. 그러나 측정 residual 자체의 median을 직접 최종 추정에 사용하는 것이 아니라, 기지국을 하나씩 제외해 만든 여러 위치 후보들의 좌표별 median을 초기 위치로 사용한다. 즉, median 기반 강건 추정 아이디어를 위치 후보 공간에 적용한 점이 다르다. |

## 3. Agent AI 활용 방안

본 프로젝트에서는 Agent AI를 단순히 코드를 대신 작성해주는 도구가 아니라, 알고리즘 후보를 비교하고 설계 방향을 검토하는 토론 도구로 활용하였다. 사용한 Agent AI는 ChatGPT이며, 알고리즘 아이디어 정리, 실험 결과 해석, 코드 구조 점검, 보고서 문장 정리에 활용하였다. 다만 최종 알고리즘의 선택과 설계 판단은 본인이 과제 조건과 중간발표 경험을 바탕으로 결정하였다.

처음에는 여러 알고리즘 후보를 비교하였다. 단순 최소제곱법, 가중 최소제곱법, 거리 기반 가중치, 기지국 조합 기반 후보 생성, Random Forest와 같은 지도학습 모델 등을 검토하였다. Agent AI는 각 방식의 장단점과 과제 조건에서의 위험 요소를 정리해주었다. 예를 들어 지도학습 모델은 제공된 700명 데이터에서는 낮은 오차를 보일 수 있지만, hidden test set에서 과적합될 수 있다는 점을 검토하였다. 반대로 기하 기반 알고리즘은 최저 RMSE만 놓고 보면 불리할 수 있지만, 알고리즘의 동작 원리가 명확하고 hidden test에도 동일한 규칙으로 적용할 수 있다는 장점이 있다고 판단하였다.

알고리즘 방향을 정하는 과정에서는 중간발표 때 사용했던 편향 보정 아이디어를 Final Project 데이터에 적용할 수 있는지 검토하였다. Agent AI는 처음에 기지국별 median bias의 25%만 제거하는 보수적인 방식을 제안하였다. 그러나 본인은 제공된 데이터에서 RTT 측정값이 실제 거리보다 전반적으로 크게 나타나는 양의 편향을 보였기 때문에, 25% 보정은 너무 약할 수 있다고 판단하였다. 이에 대해 50%에서 70% 정도의 편향 제거가 더 합리적인지 질문하였고, 여러 보정 비율을 비교한 뒤 최종적으로 50% 절반 편향 보정을 선택하였다. 이 과정에서 Agent AI의 제안을 그대로 받아들이지 않고, 데이터의 특성과 hidden test에서의 과보정 가능성을 함께 고려하여 최종 보정 비율을 결정하였다.

또한 기지국별 선형 거리 보정도 함께 검토하였다. 중간발표에서 거리 보정식을 사용했던 경험이 있었기 때문에, 각 기지국에 대해 측정거리와 실제거리 사이의 1차 보정식을 적용하면 성능이 더 좋아질 수 있다고 생각하였다. Agent AI와 함께 기지국별 선형 보정을 적용한 결과, 거리값 자체의 RMSE는 줄어들었지만 최종 위치 추정 RMSE는 오히려 증가하였다. 이를 통해 위치 추정 성능은 단순히 거리값의 평균적 정확도만으로 결정되는 것이 아니라, 가까운 기지국 선택 순서, 잔차 구조, 기지국 배치의 기하학적 조건에도 영향을 받는다는 점을 확인하였다. 따라서 제안 알고리즘에서는 선형 거리 보정이 아니라 절반 편향 보정을 사용하기로 결정하였다.

알고리즘 고도화 과정에서는 거리 역수 가중치도 검토하였다. Agent AI는 가까운 기지국을 더 신뢰하는 물리적 직관을 반영하기 위해 최종 보정 단계에서 잔차 기반 강건 가중치에 거리 역수 가중치를 곱하는 방법을 제안하였다. 그러나 실제 실험 결과, 이 방식은 일부 가까운 기지국을 과도하게 신뢰하게 만들어 최대 오차가 증가하였다. 본인은 이미 제안 알고리즘 안에 가까운 기지국 선택과 잔차 기반 기지국 재선택이 포함되어 있기 때문에, 거리 역수 가중치는 같은 정보를 중복해서 반영할 위험이 있다고 판단하였다. 따라서 최종 코드에서는 거리 역수 가중치를 제거하고, 잔차 기반의 코시형 강건 가중치만 사용하였다.

코드 작성 과정에서도 Agent AI를 활용하였다. 과제 공지의 실행 규격에 맞게 `main()` 함수가 `(2, num_user)` 형태의 numpy 배열을 반환하도록 구성해야 했고, 사용자 수를 코드에 미리 고정하지 않아야 했다. 또한 채점기가 같은 폴더에 배치하는 `DH_FR1.mat` 파일을 읽고, `p_bs`, `d_hat` 변수를 이용해 모든 사용자에 대해 위치 추정 결과를 반환하도록 코드 구조를 점검하였다. 이 과정에서 Agent AI는 파일명, 입력 배열의 shape, 반환 배열의 shape, 실행 시간 제한 등에서 오류가 발생할 수 있는 부분을 확인하는 보조 역할을 하였다.

보고서 작성 과정에서도 Agent AI를 활용하였다. 특히 알고리즘을 단순히 “가중치를 조정한 방법”으로 설명하면 다른 학생들의 방식과 유사하게 보일 수 있다고 판단하여, 제안 알고리즘의 핵심을 “기지국을 하나씩 제외하며 생성한 위치 후보의 일관성”과 “초기 위치 기준 잔차가 작은 기지국 재선택”으로 정리하였다. Agent AI는 이러한 설명을 자연어와 수식으로 정리하는 데 도움을 주었고, 본인은 과제 조건에서 금지된 코드블록, 의사코드, 이미지가 들어가지 않도록 최종 보고서 형식을 점검하였다.

정리하면, Agent AI는 본 프로젝트에서 알고리즘 후보를 제안하고, 실험 결과를 해석하며, 코드와 보고서의 구조를 점검하는 보조 도구로 활용되었다. 그러나 최종 알고리즘의 선택은 본인이 수행하였다. 특히 편향 보정 비율을 25%에서 50%로 조정한 과정, 선형 거리 보정을 최종 알고리즘에서 제외한 과정, 거리 역수 가중치를 제거한 과정은 모두 Agent AI의 제안을 비판적으로 검토하고 데이터 결과와 과제 조건을 바탕으로 판단한 결과이다. 따라서 본 프로젝트에서 Agent AI는 최종 답을 대신 만든 도구라기보다는, 알고리즘 설계 과정에서 여러 선택지를 비교하고 본인의 판단을 구체화하는 보조 역할을 수행하였다.

## 4. 결과 도출 & 디스커션

제안 알고리즘의 성능은 제공된 700명 학습 데이터에서 계산하였다. 성능 평가는 실제 위치와 추정 위치 사이의 2차원 거리 오차를 기준으로 하였다. 사용자 $u$에 대한 위치 오차는 다음과 같이 계산하였다.

$$
e_u
=
\sqrt{
(x_u-\hat x_u)^2+
(y_u-\hat y_u)^2
}
$$

RMSE는 다음과 같이 계산하였다.

$$
RMSE
=
\sqrt{
\frac{1}{N}
\sum_{u=1}^{N}
e_u^2
}
$$

제안 알고리즘의 성능 비교는 편향 보정 전 알고리즘과, 절반 편향 보정을 적용한 제안 알고리즘 사이에서 수행하였다. 두 알고리즘은 모두 지도학습 모델을 사용하지 않고, 같은 기지국 좌표와 RTT 측정값을 이용한다. 차이는 기지국별 median 편향을 보정하였는지 여부이다. 따라서 이 비교는 편향 보정이 본 알고리즘 구조 안에서 실제로 어떤 영향을 주었는지 확인하기 위한 비교로 볼 수 있다.

| Algorithm | RMSE | Mean Error | Median Error | Max Error | P(error ≤ 5m) | P(error ≤ 10m) |
|---|---:|---:|---:|---:|---:|---:|
| 편향 보정 전 알고리즘 | 12.0341 | 6.8768 | 1.5646 | 56.7875 | 64.00% | 75.71% |
| 제안 알고리즘 | 9.6416 | 7.6281 | 5.9932 | 53.7293 | 26.57% | 84.57% |

편향 보정 비율은 임의로 정한 것이 아니라, 편향을 제거하지 않은 경우와 일부만 제거한 경우, 강하게 제거한 경우를 비교하여 선택하였다. 기지국별 median 편향을 전부 제거하면 모든 위치에서 동일한 편향이 발생한다고 가정하는 것이므로 과보정 위험이 있다. 반대로 편향을 너무 적게 제거하면 RTT 측정값에 존재하는 양의 편향이 충분히 줄어들지 않는다. 따라서 본 실험에서는 편향 제거 비율에 따른 성능 변화를 함께 확인하였다.

| 편향 제거 비율 | RMSE | Mean Error | Median Error | Max Error | P(error ≤ 5m) | P(error ≤ 10m) | 해석 |
|---:|---:|---:|---:|---:|---:|---:|---|
| 0.0 | 12.0341 | 6.8768 | 1.5646 | 56.7875 | 64.00% | 75.71% | 편향 보정 없음. 작은 오차 샘플은 많지만 큰 오차가 남아 RMSE가 큼 |
| 0.25 | 9.8789 | 6.4116 | 3.3924 | 53.6386 | 68.43% | 84.00% | 보수적 보정. 평균 오차와 5m 이하 비율은 좋지만 RMSE는 0.5보다 큼 |
| 0.5 | 9.6416 | 7.6281 | 5.9932 | 53.7293 | 26.57% | 84.57% | 큰 오차를 줄여 RMSE가 가장 낮고, 10m 이하 비율도 가장 높음 |
| 0.7 | 9.9855 | 8.6778 | 7.8045 | 51.8105 | 10.86% | 81.86% | 보정이 강해지면서 중앙값 오차와 5m 이하 비율이 악화됨 |
| 1.0 | 11.3665 | 10.5079 | 10.1521 | 48.4262 | 6.14% | 47.14% | 전체 편향 제거. 최대 오차는 줄지만 전반적인 위치 정확도는 악화됨 |

위 결과를 보면 편향을 전혀 제거하지 않은 경우에는 작은 오차 샘플의 비율은 높지만 RMSE가 크다. 이는 일부 큰 오차가 전체 성능을 흔들고 있음을 의미한다. 반대로 편향을 100% 제거하면 모든 위치에서 동일한 bias가 발생한다고 가정하는 셈이 되어 과보정이 발생할 수 있다. 실제로 full bias removal에서는 median error와 10m 이하 비율이 크게 악화되었다. 따라서 본 알고리즘에서는 편향을 완전히 제거하지 않으면서도 큰 오차를 줄이는 절충값으로 0.5를 선택하였다.

결과를 보면, 절반 편향 보정을 적용한 제안 알고리즘은 편향 보정 전 알고리즘 대비 RMSE를 감소시켰다.

$$
\frac{12.0341-9.6416}{12.0341}\times100
\approx 19.9\%
$$

즉, 제공된 700명 데이터 기준으로 RMSE는 약 19.9% 감소하였다. 또한 10m 이하 오차 비율도 75.71%에서 84.57%로 증가하였다. 이 결과는 기지국별 median 편향의 일부를 제거하는 것이 큰 오차를 줄이고, 전체적인 위치 추정 안정성을 개선하는 데 도움이 되었음을 보여준다.

그러나 모든 지표가 동시에 좋아진 것은 아니다. 절반 편향 보정을 적용한 경우 median error는 1.5646에서 5.9932로 증가하였고, 5m 이하 오차 비율도 64.00%에서 26.57%로 감소하였다. 이 점은 매우 중요하다. 만약 단순히 RMSE만 보면 제안 알고리즘이 무조건 더 좋다고 해석할 수 있지만, 중앙값 오차와 5m 이하 비율을 보면 작은 오차 구간에서는 오히려 성능이 나빠진 것을 확인할 수 있다. 따라서 제안 알고리즘은 모든 구간에서 오차를 균일하게 줄이는 방식이 아니라, 위치 추정 성능을 나타내는 주요 지표 중 하나인 RMSE 개선과 이상치 차단에 집중하도록 설계된 결과물로 해석할 수 있다.

실제 RTT 측정 데이터 분석 결과, 일부 기지국에서 크게 튀는 이상치가 존재하였고, 이러한 큰 오차는 제곱합을 기반으로 하는 RMSE에 큰 영향을 준다. 제안 알고리즘은 일반적인 샘플에서의 미세 정밀도, 즉 median error를 일부 희생하더라도 큰 오차를 줄이는 방향으로 설계되었다.

그 결과 700명 기준 전체 RMSE를 약 19.9% 감소시켰을 뿐만 아니라, 10m 이하 오차 비율을 75.71%에서 84.57%로 개선하였다. 이는 소오차 구간의 정밀도와 대형 이상치 방어 사이의 공학적 트레이드오프를 데이터 관점에서 분석하고, 그에 맞춰 편향 보정 강도를 선택한 결과이다.

이러한 결과는 본인의 알고리즘 설계 의도와 어느 정도 일치한다. 제안 알고리즘은 처음부터 작은 오차 샘플의 비율을 극대화하기 위해 설계한 것이 아니라, RTT 측정값에 포함된 양의 편향과 일부 기지국의 큰 이상값이 전체 위치 추정을 흔드는 문제를 줄이기 위해 설계하였다. 즉, 핵심 목표는 일부 매우 큰 오차를 줄이고, hidden test에서도 동일하게 적용 가능한 결정론적 구조를 만드는 것이었다. 이 관점에서 RMSE 감소와 10m 이하 비율 증가는 설계 목적과 부합한다고 볼 수 있다.

반면 5m 이하 비율이 감소한 것은 제안 알고리즘의 한계로 볼 수 있다. 기지국별 median 편향의 절반을 제거하는 방식은 전체적으로 큰 오차를 줄이는 데는 효과가 있었지만, 원래 보정 전 알고리즘에서 매우 잘 맞던 일부 샘플에 대해서는 오히려 거리를 과하게 수정했을 가능성이 있다. 특히 RTT 오차가 모든 위치에서 동일한 상수 편향으로 발생하는 것은 아니기 때문에, 하나의 기지국별 median 편향을 모든 사용자에게 적용하면 일부 구간에서는 과보정이 발생할 수 있다. 따라서 제안 알고리즘은 RMSE 중심의 안정성 개선에는 장점이 있지만, 소오차 구간의 정밀도에서는 손실이 발생할 수 있다.

baseline과의 비교가 공정한지도 고려하였다. 본 보고서에서는 Random Forest, MLP와 같은 지도학습 모델을 주요 baseline으로 두지 않았다. 그 이유는 해당 모델들이 학습 데이터의 정답 좌표 $p$를 직접 사용하여 $d_{hat}$에서 $(x,y)$를 예측하기 때문이다. 반면 제안 알고리즘은 학습 데이터의 정답 좌표를 기지국별 편향 상수 계산에만 사용하고, 위치 추정 자체는 RTT 거리값과 기지국 좌표를 이용한 기하 기반 절차로 수행한다. 따라서 지도학습 모델과 직접 RMSE만 비교하면 사용한 정보량과 모델 복잡도가 달라져 공정하지 않다.

또한 단순 삼변측량 또는 전체 기지국 최소제곱법과 비교하는 것도 의미는 있지만, 본 실험의 핵심은 “편향 보정과 기지국 재선택이 기존 기하 기반 구조 안에서 어떤 효과를 가지는가”를 확인하는 것이었다. 따라서 같은 기지국 제외 일관성 구조에서 편향 보정을 적용하기 전과 적용한 후를 비교하는 방식이 제안 알고리즘의 기여를 가장 직접적으로 보여준다고 판단하였다. 이 비교는 완전히 다른 계열의 모델을 비교하는 것이 아니라, 동일한 위치 추정 구조에서 한 요소를 추가했을 때의 효과를 보는 것이므로 비교 조건이 비교적 공정하다고 보았다.

추가적으로 몇 가지 개선안도 검토하였다. 첫 번째는 거리 역수 가중치를 최종 보정 단계에 곱하는 방식이었다. 가까운 기지국을 더 신뢰한다는 직관은 타당해 보였지만, 실제 실험에서는 일부 가까운 기지국을 과도하게 신뢰하게 되어 최대 오차가 증가하였다. 제안 알고리즘은 이미 가까운 기지국 선택과 잔차 기반 기지국 재선택을 포함하고 있기 때문에, 추가적인 거리 역수 가중치는 같은 정보를 중복해서 반영하는 효과를 만들 수 있다고 판단하였다. 따라서 최종 알고리즘에서는 거리 역수 가중치를 사용하지 않았다.

두 번째는 기지국별 선형 거리 보정이었다. 각 기지국에 대해 $d_{true}\approx a_i\hat d_i+b_i$ 형태의 선형 보정식을 학습하면 거리값 자체의 RMSE는 크게 감소하였다. 그러나 이를 위치 추정 알고리즘에 적용했을 때 최종 위치 RMSE는 오히려 증가하였다. 이 결과를 통해 위치 추정 성능은 거리값의 평균적 정확도만으로 결정되지 않는다는 점을 확인하였다. 실제 위치 추정에서는 가까운 기지국의 선택 순서, 잔차 구조, 기지국 배치의 기하학적 조건도 함께 영향을 준다. 따라서 제안 알고리즘에서는 거리값 자체를 가장 잘 맞추는 선형 보정보다, 위치 추정 구조와 더 잘 맞는 절반 편향 보정을 선택하였다.

제안 알고리즘의 장점은 다음과 같다. 첫째, 기지국별 양의 편향을 일부 완화하여 큰 오차를 줄일 수 있다. 둘째, 가까운 기지국 후보군에서 기지국을 하나씩 제외하며 여러 위치 후보를 만들기 때문에 특정 기지국 하나에 과도하게 의존하지 않는다. 셋째, 후보 위치의 평균이 아니라 좌표별 중앙값을 사용하므로 일부 후보가 크게 튀어도 초기 위치가 비교적 안정적이다. 넷째, 초기 위치 기준으로 잔차가 작은 기지국을 다시 선택하므로 최종 보정 단계에서 현재 위치와 양립하기 어려운 측정값의 영향을 줄일 수 있다. 마지막으로 지도학습 모델이 아니기 때문에 별도의 모델 파일 없이 `main.py`와 `train.py`만으로 재현 가능하다.

반대로 한계도 명확하다. 첫째, 기지국별 median 편향의 절반을 제거하는 방식은 보수적인 경험적 설정이며, 모든 hidden test 샘플에 최적이라고 보장할 수는 없다. 둘째, 가까운 기지국 5개와 잔차 작은 기지국 8개를 사용하는 구조 역시 데이터 특성에 따라 달라질 수 있다. 셋째, 지도학습 모델과 비교하면 train 데이터에 대한 최저 RMSE를 달성하기는 어려울 수 있다. 넷째, RMSE는 개선되었지만 5m 이하 오차 비율은 감소했으므로, 정밀 위치 추정이 중요한 상황에서는 추가적인 보완이 필요하다.

자체 평가 방식의 fairness도 고려하였다. 본 실험에서 사용한 결과는 제공된 700명 데이터에서 계산한 것이며, hidden test 300명에 대한 성능은 직접 확인할 수 없다. 따라서 본 결과를 hidden test 성능으로 단정하지 않았다. 또한 성능을 RMSE 하나로만 판단하지 않고 mean error, median error, max error, 5m 이하 비율, 10m 이하 비율을 함께 확인하였다. 이를 통해 알고리즘이 큰 오차를 줄이는 데 유리한지, 일반적인 샘플에서 더 정확한지, 특정 구간에서 손실이 있는지 분리해서 해석하고자 하였다.

향후 개선 방향으로는 세 가지를 고려할 수 있다. 첫째, 편향 제거 비율을 모든 기지국에 동일하게 0.5로 고정하지 않고, 기지국별 오차 분포에 따라 다르게 설정하는 방법을 고려할 수 있다. 둘째, 최종 보정에 사용할 기지국 수를 고정된 8개로 두는 대신, 잔차의 median과 MAD를 이용해 동적으로 선택하는 방법을 적용할 수 있다. 셋째, 현재 알고리즘의 비선형 강건 보정 단계에서 발생하는 반복 연산 비용을 최적화하기 위해, 향후에는 야코비안(Jacobian) 행렬 계산을 경량화하여 연산 자원이 제한된 임베디드 모빌리티 보드에서도 실시간(Real-time)으로 동작 가능한 최적화 연구를 진행할 계획이다.

종합하면, 제안 알고리즘은 모든 성능 지표에서 우수한 만능 알고리즘은 아니다. 그러나 RTT 측정값의 양의 편향과 일부 기지국 이상값이 위치 추정을 흔드는 문제를 완화하기 위해 설계되었고, 제공된 데이터 기준으로 RMSE와 10m 이하 비율에서 개선을 보였다. 또한 지도학습 모델에 비해 구조가 명확하고, hidden test에서도 동일한 규칙으로 적용 가능하다는 점에서 과제 조건에 적합한 기하 기반 강건 위치 추정 알고리즘이라고 판단하였다.

## Reference

Wang, F., Tang, H., & Chen, J. “Survey on NLOS Identification and Error Mitigation for UWB Indoor Positioning.” Electronics, 12(7), 1678, 2023.

Khodjaev, J., Tedesco, S., & O’Flynn, B. “Improved NLOS Error Mitigation Based on LTS Algorithm.” Progress In Electromagnetics Research Letters, 58, 133–139, 2016.

Casas, R., Marco, A., Guerrero, J. J., & Falcó, J. “Robust Estimator for Non-Line-of-Sight Error Mitigation in Indoor Localization.” EURASIP Journal on Advances in Signal Processing, 2006.
